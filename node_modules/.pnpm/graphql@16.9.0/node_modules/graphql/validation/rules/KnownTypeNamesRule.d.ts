import type { ASTVisitor } from '.pnpm/graphql@16.9.0/node_modules/graphql/language/visitor';
import type {
    SDLValidationContext,
    ValidationContext,
} from '.pnpm/graphql@16.9.0/node_modules/graphql/validation/ValidationContext';
/**
 * Known type names
 *
 * A GraphQL document is only valid if referenced types (specifically
 * variable definitions and fragment conditions) are defined by the type schema.
 *
 * See https://spec.graphql.org/draft/#sec-Fragment-Spread-Type-Existence
 */
export declare function KnownTypeNamesRule(
  context: ValidationContext | SDLValidationContext,
): ASTVisitor;
