import axios from 'axios/unsafe/axios.js';

// This module is intended to unwrap Axios default export as named.
// Keep top-level export same with static properties
// so that it can keep same with es module or cjs
const {
  Axios,
  AxiosError,
  CanceledError,
  isCancel,
  CancelToken,
  VERSION,
  all,
  Cancel,
  isAxiosError,
  spread,
  toFormData,
  AxiosHeaders,
  HttpStatusCode,
  formToJSON,
  getAdapter,
  mergeConfig
} = axios;

export {
    all, Axios,
    AxiosError, AxiosHeaders, Cancel, CanceledError, CancelToken, axios as default, formToJSON,
    getAdapter, HttpStatusCode, isAxiosError, isCancel, mergeConfig, spread,
    toFormData, VERSION
};

