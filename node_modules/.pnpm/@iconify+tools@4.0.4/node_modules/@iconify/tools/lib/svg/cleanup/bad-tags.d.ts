import { SVG } from '@iconify/tools/lib/svg/index.js';
import '@iconify/types';
import '@iconify/utils/lib/customisations/defaults.js';
import 'cheerio';

/**
 * Options
 */
interface CheckBadTagsOptions {
    keepTitles?: boolean;
}
/**
 * Test for bag tags
 */
declare function checkBadTags(svg: SVG, options?: CheckBadTagsOptions): void;

export { checkBadTags, type CheckBadTagsOptions };

