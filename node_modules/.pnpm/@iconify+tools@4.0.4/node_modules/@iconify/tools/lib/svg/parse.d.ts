import { CheerioElement, WrappedCheerioElement } from '@iconify/tools/lib/misc/cheerio.js';
import { SVG } from '@iconify/tools/lib/svg/index.js';
import '@iconify/types';
import '@iconify/utils/lib/customisations/defaults.js';
import 'cheerio';

/**
 * Item in callback
 */
interface ParseSVGCallbackItem {
    tagName: string;
    element: CheerioElement;
    $element: WrappedCheerioElement;
    svg: SVG;
    parents: ParseSVGCallbackItem[];
    testChildren: boolean;
    removeNode: boolean;
}
/**
 * Callback function
 */
type ParseSVGCallback = (item: ParseSVGCallbackItem) => void;
/**
 * Parse SVG
 *
 * This function finds all elements in SVG and calls callback for each element.
 */
declare function parseSVG(svg: SVG, callback: ParseSVGCallback): void;

export { parseSVG, type ParseSVGCallback, type ParseSVGCallbackItem };

